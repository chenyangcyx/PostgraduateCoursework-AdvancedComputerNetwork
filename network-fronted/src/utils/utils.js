import { DateUtil } from './DataUtils';
import pinyin from './pinyin';
import {isContainsItem, getUserInfoByToken, deepClone,isStrAndIsNotBlank,isBlank} from './CommonUtils'

export default {
    dateUtil: DateUtil,
    pinyin,
    isContainsItem,
    getUserInfoByToken,
    deepClone,
    isStrAndIsNotBlank,
    isBlank
}
